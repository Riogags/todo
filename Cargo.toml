[package]
name = "todo"
version = "0.1.0"
edition = "2024"
description = "A simple CLI todo app"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
